<?xml version='1.0' encoding='utf-8'?>

<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/snaglog_test</property>
        <property name="connection.username">abriggs3</property>
        <property name="connection.password">sqlPassword</property>
        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQL5Dialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- instruction to rebuild the database after each test -->
        <property name="hbm2ddl.auto">create-drop</property>

        <!-- List the annotated entity class/es -->
        <mapping class="edu.matc.entity.User"/>
        <mapping class="edu.matc.entity.Log"/>
        <mapping class="edu.matc.entity.Snag"/>
        <mapping class="edu.matc.entity.UserRoles"/>

    </session-factory>

</hibernate-configuration>
